import news_now_BOT

# this is the main module of your app
# it is only required if your project must be runnable
# this is the script to be executed whenever some users writes `python -m news_now_BOT` on the command line, eg.
x = news_now_BOT.MyClass().my_method()
print(x)
